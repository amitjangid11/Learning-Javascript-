const course = {
    courseName: "Js in Hindi",
    coursePrice: "999",
    courseInstructor: "Hitesh Chaudhary"
}

const { courseInstructor: instrctor } = course;
console.log(courseInstructor);

// Destructuring
const navbar = ({ company }) => {

}
navbar(company = "hitesh");

// Next topic was json and api 
